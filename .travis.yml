dist: bionic

vm: 
  size: 2x-large

language: python

python:
  - 3.7
  - 3.8

install:
  - pip install -r requirements_test.txt
  - pip install tox-travis

env:
  global:
    - TF_CPP_MIN_LOG_LEVEL=2

script:
  - tox

cache: pip

branches:
  only:
  - main

after_success:
  - python -m codecov
