language: python

python:
  - 3.7
  - 3.8

install:
  - make virtualenv

env:
  global:
    - TF_CPP_MIN_LOG_LEVEL=2

script:
  - make test

cache: pip

jobs:
  fast_finish: true
  allow_failures:
     - python: 3.8

branches:
  only:
  - main

after_success:
  - make run_codecov
